import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF003580);       // Xanh đậm Booking.com
  static const Color primaryLight = Color(0xFF0071C2);  // Xanh nhạt hơn
  static const Color accent = Color(0xFFFEBA02);        // Vàng

  // Background
  static const Color background = Color(0xFFF5F5F5);
  static const Color white = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFF9E9E9E);

  // Border
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderFocus = Color(0xFFFEBA02); // Viền vàng khi focus

  // Search history icon
  static const Color historyIcon = Color(0xFF0071C2);

  // Divider
  static const Color divider = Color(0xFFEEEEEE);
}
